# ISSUE設計・コミット粒度ガイドライン (AI向け指示書)

このドキュメントは、AIに機能仕様を渡してISSUE起票やコミット単位の提案をさせる際の判断基準を定義する。AIはこのガイドラインに従って出力を行うこと。

---

## 大原則

- **1 ISSUE = 1 レビュー可能な変更**
- **1 commit = 1 論理的な変更**

サイズ・分割方針・書き方はすべてこの原則からの導出。

---

## ISSUE設計のルール

### サイズの目安

| 指標 | 目安 |
|---|---|
| 差分行数 | 400行以内 (テスト・自動生成コード除く) |
| 作業時間 | 0.5〜2日 |
| レビュー時間 | 30〜60分で読み切れる |

400行はあくまで目安。「論理的に1つのことしかしていないか」を優先する。

### 分割方針: 縦切り原則

**縦切り (機能スライス)** で分割する。**横切り (レイヤー分割) は禁止。**

- ❌ 横切り: DB層 → API層 → UI層
  - 各サブが単独で価値を生まない
  - 全部揃うまでデプロイ不可
  - 途中で切り捨てられない
- ✅ 縦切り: 最小機能 → 機能拡張 → さらなる拡張
  - 各サブが単独でマージ・デプロイ可能
  - 各段階でユーザー価値が出る
  - 優先度変更に強い

### 各サブISSUEが満たすべき条件

1. 単独でマージできる (他サブの完了を待たない)
2. 単独でデプロイできる (壊れた中間状態を生まない)
3. 単独でユーザー価値または技術的価値を生む
4. 「このサブISSUEだけ実装したら何ができるようになるか」に明確に答えられる

### 書き方ルール

1. タイトルは **動詞+結果** で書く
   - ❌ 「認証機能」「メニュー画面」
   - ✅ 「メール+パスワードでログインできる」「メニュー一覧を表示できる」
2. 完了条件 (Acceptance Criteria) を箇条書きで明示する
3. リファクタリングと機能追加を同じISSUEに混ぜない (リファクタは先行ISSUEに切り出す)
4. 1つの関心事に絞る

---

## コミット粒度のルール

### 基本方針

- **各コミットは独立して意味を持つ**: 単独でrevertできる、単独でビルド・テストが通る
- **論理単位で区切る**: 物理的な作業順序ではなく、変更の意味で区切る
- **1コミットに複数の関心事を混ぜない**: feat と refactor を同じコミットにしない

### コミットメッセージ規約 (Conventional Commits)

```
<type>: <subject>

<body (任意)>
```

| type | 用途 |
|---|---|
| feat | 新機能追加 |
| fix | バグ修正 |
| refactor | 振る舞いを変えない内部改善 |
| test | テスト追加・修正のみ |
| docs | ドキュメントのみ |
| chore | ビルド設定・依存関係など |
| style | フォーマット・空白など |

### コミット分割の指針

1コミット = 1コミットメッセージで完結に説明できる単位。以下のような分割が一般的:

- マイグレーション追加
- 新規モジュール追加 (model / service など単位)
- 既存モジュールへの機能追加
- テスト追加
- 関連するリファクタリング (機能追加コミットとは別にする)

**目安**: 1サブISSUE内で 3〜7コミット程度。1コミットあたり差分100〜200行が読みやすい。

### 禁止事項

- ❌ 「WIP」「fix」「update」のような内容のないメッセージ
- ❌ 機能追加とリファクタを混ぜたコミット
- ❌ レビュー指摘の修正を毎回別コミットにする (PRをマージする前にsquashまたはfixupで整理する)

---

## 具体例: 「メニューから注文できる」

仕様: ユーザーがメニューを見て、商品を選んで注文できるようにする。

### ❌ 悪い分割 (横切り)

- サブ1: 注文テーブルのスキーマ設計
- サブ2: 注文API実装
- サブ3: メニュー一覧画面
- サブ4: カート画面
- サブ5: 注文確定画面
- サブ6: E2Eテスト

問題: サブ1〜2だけマージしても誰も注文できない。サブ6は全部揃わないと書けない。実質的に全部マージするまで価値ゼロ。

### ✅ 良い分割 (縦切り)

#### サブISSUE 1: メニュー一覧を表示できる
- 完了条件: ユーザーがメニュー画面を開くと商品リスト (名前・価格) が表示される
- 含むもの: DBスキーマ・API・UI・テスト (すべて最小)
- 単独価値: 閲覧機能としてリリース可能

**コミット例**:
1. `feat: menu テーブルのマイグレーションを追加`
2. `feat: GET /menus エンドポイントを追加`
3. `feat: メニュー一覧画面を追加`
4. `test: メニュー一覧表示のE2Eテストを追加`

#### サブISSUE 2: 1商品を即時注文できる (カートなし)
- 完了条件: 商品を選んで「注文する」を押すと注文が確定し、店舗に通知が届く
- 数量は1固定、決済は仮実装可
- 単独価値: MVPとしてリリース可能 (注文機能の最小成立)

**コミット例**:
1. `feat: order テーブルのマイグレーションを追加`
2. `feat: POST /orders エンドポイントを追加`
3. `feat: 商品詳細画面に注文ボタンを追加`
4. `feat: 店舗向け注文通知を追加`
5. `test: 単品注文のE2Eテストを追加`

#### サブISSUE 3: カートに複数商品を入れて一括注文できる
- 完了条件: 複数商品をカートに追加し、まとめて注文できる
- 単独価値: 利便性の向上

#### サブISSUE 4: カート内で数量変更・削除ができる
- 完了条件: カート画面で数量変更・商品削除が可能

#### サブISSUE 5: 注文履歴を確認できる
- 完了条件: 過去の注文一覧を表示できる

### サイズオーバー時のさらなる分割

サブISSUE 2 が400行を超えそうなら、さらに縦に切る:

- 2a: 注文を作成できる (確認画面なし、押したら即確定)
- 2b: 確認画面を挟んで注文できる
- 2c: 注文完了後にサンクスページを表示する

各段階で「前より体験が良くなる」ようにする (Walking Skeleton から肉付けするイメージ)。

---

## AIへの作業指示

### ISSUE作成タスクが来たとき

1. **仕様の確認**: 仕様が曖昧ならまずユーザーに質問する。前提を勝手に補完しない。
2. **縦切りでサブISSUEを設計する**: 各サブが単独で価値を持つことを確認する。
3. **以下の形式で出力する**:

```markdown
## 親ISSUE: <動詞+結果のタイトル>

### 背景・目的
<なぜこの機能が必要か>

### サブISSUE一覧

#### サブ1: <動詞+結果のタイトル>
**完了条件**:
- [ ] <検証可能な条件1>
- [ ] <検証可能な条件2>

**含むもの**: DB / API / UI / テスト

**単独価値**: <これ単独でユーザーまたは開発者に何が提供されるか>

**想定コミット**:
1. <type>: <subject>
2. <type>: <subject>
...

#### サブ2: ...
```

4. **セルフチェック**: 出力前に下記の全項目をパスしているか確認する。

### コミット単位を提案するタスクが来たとき

1. **対象サブISSUEのスコープを確認する**: 何を作るのか、何を作らないのかを明確にする。
2. **論理単位でコミットを区切る**:
   - マイグレーション
   - モデル / サービス / コントローラなどモジュール単位
   - UI追加
   - テスト追加
   - 必要なら先行リファクタリング
3. **Conventional Commits 形式でメッセージを提案する**。
4. **各コミットが単独で意味を持つか確認する**: 「このコミットだけrevertしたとき、ビルドが通るか?」を自問する。

---

## セルフチェック (出力前に必ず確認)

### ISSUE設計のチェック
- [ ] 各サブISSUEは単独でマージ・デプロイ可能か
- [ ] 各サブISSUEは単独でユーザー価値または技術的価値を生むか
- [ ] 横切り (レイヤー分割) になっていないか
- [ ] タイトルは動詞+結果で書かれているか
- [ ] 完了条件は検証可能な形で書かれているか
- [ ] リファクタと機能追加が混ざっていないか
- [ ] 1サブISSUEが400行を超えそうなら、さらに分割を検討したか

### コミットのチェック
- [ ] 各コミットは1つの関心事だけを扱っているか
- [ ] 各コミットは単独でビルド・テストが通る状態か
- [ ] コミットメッセージは Conventional Commits 形式で、内容を正確に表しているか
- [ ] 機能追加とリファクタが同じコミットに混ざっていないか

---

## 例外を許容するケース

以下はサイズや分割ルールを超過してもよい。ただしISSUE/PRのdescriptionに理由を明記する。

- 大規模な自動リネーム・フォーマット適用 (機械的変更でレビュー負荷が低い)
- 外部ライブラリのバージョンアップに伴う一括修正
- 設計上、原子的にコミットしないと壊れる変更 (滅多にない)
- スパイク・PoCで分割するとかえってコストが上がる場合 (本番マージ前に整理する)

---

## 補足: 縦切りに迷ったときの問い

> 「このサブISSUEだけマージしたら、ユーザー (または開発者) は何ができるようになる?」

この問いに **動詞で** 答えられないなら横切りになっている可能性が高い。

- ❌ 「DBができる」「APIができる」 → 横切り
- ✅ 「メニューを見られる」「単品で注文できる」 → 縦切り

迷ったら必ずこの問いに戻ること。
